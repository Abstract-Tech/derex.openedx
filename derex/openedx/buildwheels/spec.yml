builder:
  class: derex.builder.builders.BuildahBuilder

source:
  type: derex-relative
  path: base

scripts:
  - install_build_dependencies.sh
  - patch_mariadb.sh

dest: derex/openedx-alpine-buildwheels
