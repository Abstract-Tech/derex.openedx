builder:
  class: derex.builder.builders.BuildahBuilder

source:
  type: derex-relative
  path: dev

copy:
  cleanup_assets.sh: /openedx/bin/cleanup_assets.sh

# This image is temporary.
# It should be generated from openedx-ironwood-nostatic and openedx-ironwood-dev
# but we'll do it in a second step
scripts:
  - install_ironwood.sh

dest: derex/openedx-ironwood
