builder:
  class: derex.builder.builders.BuildahBuilder

source:
  type: derex-relative
  path: dev

# This image is temporary.
# It should be generated from openedx-juniper-nostatic and openedx-juniper-dev
# but we'll do it in a second step
scripts:
  - install_juniper.sh

dest: derex/openedx-juniper
